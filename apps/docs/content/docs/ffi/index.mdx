---
title: FFI Overview
description: "The current native library system: C ABI only, static-linking first, generated bindings, and hybrid/native support."
---

Kira's first real FFI system is intentionally narrow and explicit.

## Current Contract

The supported path today is:

- C ABI only
- static linking first
- per-library TOML manifests under nearby `native_libs/`
- Clang-driven autobinding generation
- generated bindings emitted as real Kira source files
- direct extern/native calls with no public shim or wrapper API
- explicit callback support
- LLVM/native and hybrid as the intended FFI-capable pipelines

## What Is Not The First-Version Contract

Do **not** treat these as implemented public promises:

- libffi-backed runtime interop
- dynamic linking as the documented first-class workflow
- a stable Kira ABI for third-party binary plugins
- non-C ABIs
- variadics
- captured closures crossing the boundary

The manifest data model contains room for more later, but the documented and tested repo path is the one listed above.

## Where FFI Lives In The Repo

The current split is deliberate:

- `kira_native_lib_definition`: native library contracts
- `kira_manifest`: TOML parsing
- `kira_build`: manifest discovery, host resolution, archive building, and autobinding generation
- `kira_llvm_backend`: direct extern lowering and native/hybrid bridge wrapper emission
- `kira_native_bridge` and `kira_hybrid_runtime`: runtime/native marshalling

## Real Proof Targets

The repo does not stop at a tiny callback toy.

Real checked-in proof targets include:

- native callbacks
- hybrid callbacks
- generated Sokol graphics bindings
- a native triangle program
- a hybrid Sokol call proof

Read [Examples Overview](/docs/examples) and [Sokol Triangle](/docs/examples/sokol-triangle) after this page if you want the end-to-end examples.
