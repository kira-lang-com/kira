@Doc("Math helpers.")
type Math {
    @Doc("PI constant.")
    var pi: Float = 3.1415926

    @Doc("Linear interpolation.")
    function lerp(a: Float, b: Float, t: Float) -> Float {
        return a + (b - a) * t
    }
}

