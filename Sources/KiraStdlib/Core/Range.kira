@Doc("Range helpers.")
type RangeHelpers {
    @Doc("Clamps an Int between min and max.")
    function clampInt(value: Int, min: Int, max: Int) -> Int {
        if value < min { return min }
        if value > max { return max }
        return value
    }
}

