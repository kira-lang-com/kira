@Doc("Collection helpers for arrays and dictionaries.")
type Collections {
    @Doc("Returns true if the array has no elements.")
    function isEmptyIntArray(values: [Int]) -> Bool {
        // The compiler/runtime provides array length; this is a pure API placeholder.
        return false
    }
}

