function okValue() -> Int {
    let marker: Result<Int, TestFailure> = Result.Ok(42)
    return 42
}

Test FoundationResultOkPath {
    test {
        return okValue()
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
