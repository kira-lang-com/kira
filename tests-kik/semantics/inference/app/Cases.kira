function inferredAnswer() -> Int {
    let value = 40
    let offset = 2
    return value + offset
}

Test InferenceOkResult {
    test {
        return inferredAnswer()
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
