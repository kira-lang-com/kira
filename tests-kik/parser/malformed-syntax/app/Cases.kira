Test ParserExpectedDiagnostic {
    test {
        let value = ;
        return value
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Error(TestFailure.Diagnostic(KiraError.KPAR002))
        return expected
    }
}
