function shaderTargetScore(target: borrow ShaderTarget) -> Int {
    let selected = target
    return 42
}

Test KslModelOkPath {
    test {
        return shaderTargetScore(ShaderTarget.Wgsl)
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
