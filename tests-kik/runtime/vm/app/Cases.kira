Test DivideByZeroExpectedFailure {
    test {
        let zero = 0
        return 10 / zero
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Error(TestFailure.Runtime("division by zero"))
        return expected
    }
}
