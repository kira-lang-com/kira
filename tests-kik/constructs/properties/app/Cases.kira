construct Page {
    properties {
        required title: String
    }
}

Page Home {
    properties {
        title: "Home"
    }
}

Test ConstructPropertiesOkResult {
    test {
        return 42
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
