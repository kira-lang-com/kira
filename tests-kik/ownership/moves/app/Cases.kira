function consume(value: String) -> Int {
    return 42
}

Test MoveOkResult {
    test {
        let value = "owned"
        return consume(move value)
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
