function autobindScore(api: borrow AutobindApi) -> Int {
    let selected = api
    return 42
}

Test AutobindingModelOkPath {
    test {
        return autobindScore(AutobindApi.Vulkan)
    }

    expect {
        let expected: Result<Int, TestFailure> = Result.Ok(42)
        return expected
    }
}
