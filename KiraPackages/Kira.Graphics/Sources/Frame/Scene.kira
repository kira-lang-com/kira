@Doc("A scene that can respond to graphics lifecycle events.")
protocol Scene {
    function onLoad(device: GraphicsDevice)
    function onFrame(frame: Frame)
    function onResize(width: Int, height: Int)
    function onUnload()
}
