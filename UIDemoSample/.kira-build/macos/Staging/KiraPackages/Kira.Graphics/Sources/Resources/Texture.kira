@Doc("Opaque GPU texture handle.
Textures are represented as lightweight wrapper values on the Kira side.
Additional fields can grow here as texture creation and sampling APIs expand.")
type Texture { }
