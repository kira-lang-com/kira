@Doc("Opaque shader resource placeholder.
The first graphics slice compiles shaders directly into pipelines rather than exposing rich shader APIs.
This type keeps the future public surface stable while implementation grows.")
type Shader { }
