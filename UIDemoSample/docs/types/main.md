# main

**Kind:** Function
**Module:** UIDemoSample

Run the sample Foundation app in headless mode.
The app performs layout, dispatches an empty input snapshot, and builds a draw list.
Its printed summary is used by the integration test to ensure the example stays working.
