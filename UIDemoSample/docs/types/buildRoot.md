# buildRoot

**Kind:** Function
**Module:** UIDemoSample

Build the widget tree used by the sample Foundation app.
The example uses text, stacks, and boxes to produce a small dashboard-like layout.
It is designed to run headlessly through `UIFoundation.update()` and print a stable summary.
