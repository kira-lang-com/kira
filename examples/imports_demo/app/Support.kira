struct SupportQueue {
    let name: String
    let openCount: Int
    let closedCount: Int
}

struct SupportDashboard {
    let primary: SupportQueue
    let secondary: SupportQueue
}

function stub() {
    return
}
