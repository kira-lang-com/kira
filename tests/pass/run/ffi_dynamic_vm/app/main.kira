@Main
function main() {
    let sum: I32 = kira_ffi_add(40, 2)
    print(sum)
    let scaled: F64 = kira_ffi_scale(10.0)
    print(scaled)
    let length: I64 = kira_ffi_strlen("hello, kira ffi")
    print(length)
    return
}
