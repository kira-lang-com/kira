backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM060"
diagnostic_title = "duplicate annotation declaration"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
