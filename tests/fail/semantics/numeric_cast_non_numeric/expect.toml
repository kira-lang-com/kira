backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM118"
diagnostic_title = "invalid numeric cast"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
