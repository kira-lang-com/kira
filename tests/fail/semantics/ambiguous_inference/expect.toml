backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM029"
diagnostic_title = "type inference is ambiguous"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
