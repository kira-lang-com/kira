backends = ["hybrid", "vm", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM100"
diagnostic_title = "non-exhaustive match"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
