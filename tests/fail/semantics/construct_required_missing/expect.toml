backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM120"
diagnostic_title = "missing required function"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
