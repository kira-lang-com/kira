backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM064"
diagnostic_title = "annotation does not accept parameters"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
