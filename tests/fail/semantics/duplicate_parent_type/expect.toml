backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM051"
diagnostic_title = "duplicate parent type"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
