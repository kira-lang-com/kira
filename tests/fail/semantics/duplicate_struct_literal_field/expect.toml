backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM080"
diagnostic_title = "duplicate struct field"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
