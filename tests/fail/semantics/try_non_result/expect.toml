backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM134"
diagnostic_title = "'try' on non-Result value"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
