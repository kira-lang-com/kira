backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM057"
diagnostic_title = "ambiguous inherited field lookup"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
