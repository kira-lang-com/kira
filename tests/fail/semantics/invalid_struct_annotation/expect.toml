backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM096"
diagnostic_title = "invalid struct annotation"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
