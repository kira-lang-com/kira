backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM132"
diagnostic_title = "invalid content composition"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
