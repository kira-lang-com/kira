backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM023"
diagnostic_title = "invalid @Main signature"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
