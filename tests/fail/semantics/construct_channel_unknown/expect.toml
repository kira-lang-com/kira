backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM127"
diagnostic_title = "unknown content channel"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
