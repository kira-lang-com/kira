backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM136"
diagnostic_title = "handle case is not a failure variant"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
