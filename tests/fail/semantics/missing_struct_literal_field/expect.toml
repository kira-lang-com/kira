backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM081"
diagnostic_title = "missing required struct field"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
