backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM128"
diagnostic_title = "content into sealed construct"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
