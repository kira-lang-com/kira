backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM045"
diagnostic_title = "invalid callback signature"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
