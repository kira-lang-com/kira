backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM135"
diagnostic_title = "missing handle case for reachable failure variant"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
