backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM021"
diagnostic_title = "invalid lifecycle hook"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
