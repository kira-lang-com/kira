backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM089"
diagnostic_title = "nativeRecover requires RawPtr"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
