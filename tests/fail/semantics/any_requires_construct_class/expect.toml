backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM097"
diagnostic_title = "any requires a construct"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
