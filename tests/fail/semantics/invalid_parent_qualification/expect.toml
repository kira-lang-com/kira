backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM061"
diagnostic_title = "invalid parent-qualified member reference"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
