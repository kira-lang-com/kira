backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM002"
diagnostic_title = "multiple @Main entrypoints"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
