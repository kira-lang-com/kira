backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM091"
diagnostic_title = "native state type mismatch"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
