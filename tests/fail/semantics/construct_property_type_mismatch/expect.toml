backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM125"
diagnostic_title = "property type mismatch"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
