backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM121"
diagnostic_title = "required function signature mismatch"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
