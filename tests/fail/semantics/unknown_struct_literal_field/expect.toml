backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM082"
diagnostic_title = "unknown struct field"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
