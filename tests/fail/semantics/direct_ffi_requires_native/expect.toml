backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM093"
diagnostic_title = "direct FFI requires @Native"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
