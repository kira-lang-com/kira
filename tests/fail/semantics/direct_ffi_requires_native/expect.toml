# The VM now executes direct FFI through LibFFI (see tests/pass/run/ffi_dynamic_vm),
# so the @Native requirement only applies to the native-compiling backends.
backends = ["hybrid", "llvm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM093"
diagnostic_title = "direct FFI requires @Native"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
