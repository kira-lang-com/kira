backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM004"
diagnostic_title = "duplicate @Main annotation"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
