backends = ["hybrid", "vm"]

[phases.check]
result = "fail"
diagnostic_code = "KSEM137"
diagnostic_title = "incompatible failure enums in a single 'attempt'"
stage = "semantics"

[phases.build]
result = "blocked"

[phases.run]
result = "blocked"
